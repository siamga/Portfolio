영업손실 기준 30개 변수 확인 결과 16_18,15_17년도만 그나마 데이터 개수가 충분해서 결과치를 보여주는데
16_18의 경우 aic값도 보이지 않는다.
추가적으로 우리가 같은 값이라고 생각했던 p값들이 실제로는 다른 값으로 보인다. 
다만, 그 차이가 매우 작아서 같은 값으로 표시된 것으로 생각된다.
실제 16_18년도에서 유동자산/총자산과 고정비율의 p값이 동일 한 것으로 보이지만 
실제 출력 값이 0.000001 차이가 나는 것을 확인할 수 있었다. 
(최대 p값을 보여주는 코드에서 실제 최대값이 같다면 해당 값들을 모두 보여준다.(p값이 모두 0일때 모든 인덱스를 출력하는 것을 확인))